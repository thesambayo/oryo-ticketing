import { TicketStatusBadgeVariantPipe } from './ticket-status-badge-variant.pipe';

describe('TicketStatusBadgeVariantPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketStatusBadgeVariantPipe();
    expect(pipe).toBeTruthy();
  });
});
