import { TicketPriorityDisplayPipe } from './ticket-priority-display.pipe';

describe('TicketPriorityDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketPriorityDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
