import { TicketStatusDisplayPipe } from './ticket-status-display.pipe';

describe('TicketStatusDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketStatusDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
